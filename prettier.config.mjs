/** @type {import('prettier').Config} */
const config = {
  arrowParens: 'always',
  printWidth: 100,
  proseWrap: 'preserve',
  semi: false,
  singleQuote: true,
  trailingComma: 'all',
}

export default config
